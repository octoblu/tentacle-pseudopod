cmake_minimum_required (VERSION 2.8)
project (tentacle-protocol-buffer)
SET(CMAKE_CXX_FLAGS "-std=c++0x")
include_directories(include)
add_subdirectory(lib/tentacle)
add_subdirectory(src)
enable_testing()
add_subdirectory(test)
